{
  "name": "@frourio-demo/utils",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "update": "npx npm-check-updates -u --timeout 100000 && yarn install && yarn upgrade && yarn audit"
  },
  "dependencies": {},
  "devDependencies": {
    "aspida": "^1.3.0",
    "axios": "^0.21.1",
    "date-fns": "^2.16.1",
    "react": "^17.0.1",
    "typescript": "^4.1.3"
  },
  "engines": {
    "yarn": "1.x"
  }
}
